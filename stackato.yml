name: flaskbars-lsblakk
framework:
  type: buildpack
services:
  flaskbars-db: postgresql
hooks:
  pre-running: python manage.py createDbSchema
requirements:
  pypm:
    - Flask-Script=0.5.3
    - Flask=0.9
    - Flask-SQLAlchemy
    - psycopg2
    - simplejson
